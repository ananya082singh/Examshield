{
  "name": "examshield-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Secure Exam Distribution & Leak Traceability Platform Monorepo",
  "scripts": {
    "setup": "npm install --prefix backend && npm run prisma:generate --prefix backend && npm run prisma:migrate --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "verify": "npm run verify --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "puppeteer-core": "^25.1.0"
  }
}
